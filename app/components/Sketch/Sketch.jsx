var React = require('react');

var Sketch = React.createClass({
  render : function(){
    return(
      <div className="sketch display-flex">
        All sketch files
      </div>
    );
  }
});

module.exports = Sketch;
